const attachMemoryWallLimit=()=>{document.querySelectorAll('.memory-grid').forEach(grid=>{const cards=[...grid.querySelectorAll(':scope > article:not(.memory-see-more)')];cards.forEach((card,i)=>{card.style.display=i<5?'':'none'});let more=grid.querySelector(':scope > .memory-see-more');if(cards.length>5){if(!more){more=document.createElement('a');more.className='memory-see-more';more.href='/memories';grid.appendChild(more)}more.innerHTML=`<span>See More</span><small>+ ${cards.length-5} more memories</small>`;more.style.display='flex'}else if(more)more.remove()})};const observeMemoryWall=()=>{attachMemoryWallLimit();const observer=new MutationObserver(attachMemoryWallLimit);document.querySelectorAll('.memory-grid').forEach(grid=>observer.observe(grid,{childList:true}));window.addEventListener('beforeunload',()=>observer.disconnect(),{once:true})};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',observeMemoryWall);else observeMemoryWall();